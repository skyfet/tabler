name: flutter_tabler
description: Just the Tabler.
version: 0.3.3
homepage: https://github.com/skyfet/tabler

environment:
  sdk: ">=3.2.0 <3.6.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # state management
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: